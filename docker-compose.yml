version: "3.9"

services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"

  web:
    build: .
    environment:
      REDIS_URL: redis://redis:6379/0
      SECRET_KEY: change-me
    ports:
      - "5000:5000"
    depends_on:
      - redis
