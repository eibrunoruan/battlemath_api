version: "3.9"

services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"

  web:
    build: .
    env_file:
      - .env
    environment:
      - DATABASE_URL
      - REDIS_URL
      - SECRET_KEY
    ports:
      - "5000:5000"
    depends_on:
      - redis
